name: Terraform CI

# main / master ブランチへの push と PR 時に実行
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

permissions:
  id-token: write      # OIDC トークン取得（AWS 認証）
  contents: read       # リポジトリ読み取り
  pull-requests: write # PR へのコメント投稿

jobs:
  terraform:
    name: Fmt / Validate / Plan
    runs-on: ubuntu-latest

    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    defaults:
      run:
        working-directory: terraform

    steps:
      # ── 1. ソースコード取得 ────────────────────────────────
      - name: Checkout
        uses: actions/checkout@v7

      # ── 2. Terraform セットアップ ─────────────────────────
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: "~1.9"

      # ── 3. フォーマットチェック（AWS 不要）────────────────
      - name: Terraform Format
        run: terraform fmt --check -recursive

      # ── 4. 初期化（バックエンドなし）─────────────────────
      - name: Terraform Init
        run: terraform init -backend=false

      # ── 5. 構文・設定バリデーション（AWS 不要）───────────
      - name: Terraform Validate
        run: terraform validate

      # ── 5.5. Terraform Test（mock_provider で AWS 不要）──
      - name: Terraform Test
        run: terraform test

      # ── 6. AWS 認証（OIDC / AWS_ROLE_ARN が設定済みの場合）
      - name: Configure AWS credentials
        if: vars.AWS_ROLE_ARN != ''
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ap-northeast-1

      # ── 7. Terraform Plan（AWS_ROLE_ARN が設定済みの場合）
      - name: Terraform Plan
        if: vars.AWS_ROLE_ARN != ''
        id: plan
        run: |
          terraform plan \
            -var="alert_email=ci@example.com" \
            -no-color \
            -input=false
        continue-on-error: true

      # ── 8. Plan 結果を PR にコメント ──────────────────────
      - name: Comment Plan on PR
        if: github.event_name == 'pull_request' && vars.AWS_ROLE_ARN != ''
        uses: actions/github-script@v9
        with:
          script: |
            const plan = `${{ steps.plan.outputs.stdout }}`;
            const status = '${{ steps.plan.outcome }}' === 'success' ? '✅' : '❌';
            const body = [
              `### ${status} Terraform Plan`,
              '```hcl',
              plan.substring(0, 60000),
              '```',
            ].join('\n');
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body,
            });

  python-test:
    name: Python ユニットテスト
    runs-on: ubuntu-latest

    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
      AWS_DEFAULT_REGION: ap-northeast-1

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'

      - name: Install test deps
        run: pip install pytest boto3 botocore pytest-cov

      - name: pytest guardduty-notifier（24件）
        run: pytest lambda/guardduty-notifier/test_index.py -v --cov=lambda/guardduty-notifier --cov-report=xml:coverage-guardduty.xml

      - name: pytest streams-alert（11件）
        run: pytest lambda/streams-alert/test_index.py -v --cov=lambda/streams-alert --cov-report=xml:coverage-streams.xml

  security:
    name: Checkov セキュリティスキャン
    runs-on: ubuntu-latest

    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Run Checkov
        uses: bridgecrewio/checkov-action@v12
        with:
          directory: terraform
          framework: terraform
          soft_fail: false
          # SNS KMS（alias/aws/sns）は修正済み（CKV_AWS_26）
          # 以下は dev/PoC として意図的に省略
          # CKV_AWS_300: Checkov 3.x バグ - S3 ライフサイクルチェックが DynamoDB に誤適用・インライン skip 未対応
          # CKV_AWS_119: DynamoDB KMS CMK - dev/PoC は AWS 管理キーで十分（本番のみ CMK 必須）
          skip_check: >-
            CKV_AWS_91,CKV_AWS_118,CKV_AWS_119,CKV_AWS_338,CKV_AWS_300
          output_format: cli
